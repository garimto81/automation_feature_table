# Poker Hand Auto-Capture System - Production Dependencies
# For Docker deployment on Synology NAS

# Core
phevaluator>=0.5.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# File watching (NAS/SMB support)
watchdog>=3.0.0
aiofiles>=23.0.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# HTTP client (for vMix API)
httpx>=0.25.0
aiohttp>=3.9.0

# WebSocket (legacy mode)
websockets>=12.0

# Video capture (optional - for Gemini secondary)
# opencv-python>=4.8.0  # Uncomment if using video capture
